[build-system]
requires = ["setuptools>=64", "wheel", "pybind11>=2.10"]
build-backend = "setuptools.build_meta"

[project]
name = "parallelnet_cpp"
version = "0.0.1"
description = "Pybind11 bridge for the distributed parallel neural network engine"
requires-python = ">=3.8"
